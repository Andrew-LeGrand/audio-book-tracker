export class Book {
    constructor(
        public title: string,
        public author: string,
        public coverImagePath: string,
        public length: string,
        public notes?: string,
        public genre?: string,
    ) {}
}

// work on book component layout
// work on library component layout
// work on creating a new book (rails, then postman, then angular)